<Window x:Class="ZenPlatform.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Magictok 登入" Height="220" Width="360"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BrushWindowBackground}"
        Foreground="{StaticResource BrushText}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="身分證字號" VerticalAlignment="Center" />
        <TextBox x:Name="UserIdBox" Grid.Row="0" Grid.Column="1" Height="26" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="密碼" VerticalAlignment="Center" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Grid.Column="1" Height="26" />

        <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Text="註冊Magistock帳號"
                   HorizontalAlignment="Right" FontSize="13" Cursor="Hand"
                   MouseLeftButtonUp="OnRegisterClick">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource BrushInputBorder}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Margin="0,0,8,0" Click="OnCancelClick" />
            <Button Content="登入" Click="OnLoginClick" />
        </StackPanel>
    </Grid>
</Window>
