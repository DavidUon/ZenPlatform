<Window x:Class="ZenPlatform.SessionEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="建立任務"
        Width="320"
        Height="140"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BrushSurface}">
    <Border Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="選擇任務方向"
                       FontSize="18"
                       Margin="0,0,0,12"
                       HorizontalAlignment="Center" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                <Button Content="多單"
                        Width="90"
                        Margin="0,0,12,0"
                        Foreground="{StaticResource BrushTabRealTrade}"
                        Click="OnLongClick" />
                <Button Content="空單"
                        Width="90"
                        Foreground="{StaticResource BrushMarketOpen}"
                        Click="OnShortClick" />
            </StackPanel>

            <TextBlock Grid.Row="2"
                       Text="警告：點擊後將立即下單，不再確認"
                       Foreground="{StaticResource BrushDdeWarning}"
                       FontSize="12"
                       HorizontalAlignment="Center" />
        </Grid>
    </Border>
</Window>
