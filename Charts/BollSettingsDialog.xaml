<Window x:Class="Charts.BollSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="布林通道參數設定" WindowStartupLocation="CenterOwner" SizeToContent="WidthAndHeight"
        ResizeMode="NoResize" Background="#101014" Foreground="#E8E8E8"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType" SnapsToDevicePixels="True">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBg" Color="#18181D"/>
        <SolidColorBrush x:Key="InputBg" Color="#202027"/>
        <SolidColorBrush x:Key="BorderDark" Color="#3C3C46"/>
        <SolidColorBrush x:Key="BrushButtonBackground" Color="#2A2F3A"/>
        <SolidColorBrush x:Key="BrushButtonBorder" Color="#3C3C46"/>
        <SolidColorBrush x:Key="BrushText" Color="#E8E8E8"/>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style x:Key="StrategyButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrushButtonBackground}" />
            <Setter Property="Foreground" Value="{StaticResource BrushText}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushButtonBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,2" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF787878" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF606060" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SwatchButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="BorderBrush" Value="#2A2A33"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource PanelBg}" CornerRadius="6" Margin="12" Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="期間 (N)：" Margin="0,0,8,10"/>
            <TextBox x:Name="PeriodBox" Grid.Column="1" Margin="0,0,0,10"/>

            <TextBlock Text="K 倍數：" Grid.Row="1" Margin="0,0,8,10"/>
            <TextBox x:Name="KBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

            <TextBlock Text="填充顏色：" Grid.Row="2" Margin="0,0,8,10"/>
            <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" Orientation="Horizontal">
                <Button x:Name="FillSwatch" Width="36" Height="22" Margin="0,0,8,0"
                        Click="PickFillColor_Click" Style="{StaticResource SwatchButtonStyle}"/>
            </StackPanel>

            <TextBlock Text="中線顏色：" Grid.Row="3" Margin="0,0,8,10"/>
            <StackPanel Grid.Row="3" Grid.Column="1" Margin="0,0,0,10" Orientation="Horizontal">
                <Button x:Name="MidSwatch" Width="36" Height="22" Margin="0,0,8,0"
                        Click="PickMidColor_Click" Style="{StaticResource SwatchButtonStyle}"/>
            </StackPanel>

            <TextBlock Text="上下緣顏色：" Grid.Row="4" Margin="0,0,8,10"/>
            <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,0,0,10" Orientation="Horizontal">
                <Button x:Name="EdgeSwatch" Width="36" Height="22" Margin="0,0,8,0"
                        Click="PickEdgeColor_Click" Style="{StaticResource SwatchButtonStyle}"/>
            </StackPanel>

            <TextBlock Text="不透明度：" Grid.Row="5" Margin="0,0,8,10"/>
            <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal">
                <Slider x:Name="OpacitySlider" Minimum="0" Maximum="100" Width="160" Value="20"/>
                <TextBlock x:Name="OpacityText" Margin="8,0,0,0" VerticalAlignment="Center" Text="20%"/>
            </StackPanel>

            <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="90" Margin="4" Content="確定" IsDefault="True" Click="Ok_Click" Style="{StaticResource StrategyButtonStyle}"/>
                <Button Width="90" Margin="4" Content="取消" IsCancel="True" Style="{StaticResource StrategyButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
